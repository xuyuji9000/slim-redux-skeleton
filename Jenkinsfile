pipeline {
  agent {
    docker 'node:8.2'
  }
  stages {
    stage('Build') {
      steps {
        sh 'ls -lat'
        //sh 'npm install'
      }
    }
    stage('Test') {
      steps {
        sh 'ls -lat'
        sh 'ls -lat'
        sh 'pwd'
      }
    }
  }
}
